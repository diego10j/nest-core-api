import { DataSourceService } from '../connection/datasource.service';
import { LoginUserDto } from './dto/login-user.dto';
import { JwtService } from '@nestjs/jwt';
import { AuditService } from '../audit/audit.service';
import { ServiceDto } from '../../common/dto/service.dto';
import { ConfigService } from '@nestjs/config';
export declare class AuthService {
    private readonly dataSource;
    private readonly audit;
    private readonly configService;
    private readonly jwtService;
    constructor(dataSource: DataSourceService, audit: AuditService, configService: ConfigService, jwtService: JwtService);
    login(loginUserDto: LoginUserDto): Promise<{
        accessToken: string;
        ide_usua: any;
        ide_empr: any;
        ide_perf: any;
        perm_util_perf: any;
        nom_perf: string;
        id: string;
        displayName: string;
        email: any;
        login: any;
        photoURL: string;
        phoneNumber: string;
        country: string;
        address: string;
        state: string;
        city: string;
        zipCode: string;
        about: string;
        role: string;
        isPublic: boolean;
        menu: any[];
        lastAccess: string;
    }>;
    private getMenuByRol;
    private getMenuItem;
    private getLastAccessUser;
    private getJwtToken;
    logout(serviceDto: ServiceDto): Promise<import("../connection/interfaces/resultQuery").ResultQuery>;
}
